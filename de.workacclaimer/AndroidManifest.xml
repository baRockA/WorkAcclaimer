<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.workacclaimer"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:configChanges="orientation"
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name=".JobListActivity"
            android:label="@string/joblist"></activity>
        <activity android:name=".JobViewerActivity"></activity>
        <activity android:name=".SettingsActivity" android:label="@string/pref"></activity>
        <activity android:name=".WorkGroupsActivity"></activity>
        <activity android:name=".GroupViewerActivity"></activity>
    </application>

</manifest>